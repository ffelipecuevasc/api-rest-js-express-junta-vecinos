import Vecino from '../models/Vecino.js';
import Cuota from '../models/Cuota.js';

export const obtenerCuotas = async (req, res) => {
    res.status(501).json({ message: "No implementado: GET /cuotas" });
};